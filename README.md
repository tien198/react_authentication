## Continue  react_router-dom-advance

`link:` `https://github.com/tien198/react_router-dom-advance`

Project contain both backend & frontend

We can install concurrency both using concurrency library:

`npm i`

`npm start`